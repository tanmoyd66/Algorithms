package trd.algorithms.misc;

// Given input which is vector of log entries of some online system 
// each entry is something like (user_name, login_time, logout_time), 
// come up with an algorithm with outputs number of users logged in the 
// system at each time slot in the input, output should contain 
// only the time slot which are in the input. 
// For the example given below output should contain timeslots 
// [ ("Jane", 1.2, 4.5), ("Jin", 3.1, 6.7), ("June", 8.9, 10.3) ] 
// [(1.2, 1), (3.1, 2), (4.5, 1), (6.7, 0), (8.9, 1), (10.3, 0)] 


public class Problem1 {

}
